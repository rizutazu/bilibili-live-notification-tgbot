version: "3.0"
services:
  bililivenotibot:
    build: .
    volumes:
      - .:/usr/src/bilibili-live-notification-tgbot
    environment:
      - BILILIVENOTIBOT_DEBUG
    logging:
      driver: "json-file"
      options:
          max-size: "10m"
